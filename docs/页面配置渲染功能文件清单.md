# 页面配置渲染功能 - 文件清单

本文档列出了实施页面配置渲染功能过程中所有创建和修改的文件。

## 📁 目录结构

```
lowcode-platform/
├── backend/                           # 后端代码
│   └── src/main/java/com/lowcode/
│       ├── entity/
│       │   └── LowPageConfig.java     ✏️ 修改
│       ├── controller/
│       │   └── PageConfigController.java  ✏️ 修改
│       └── service/
│           ├── ILowPageConfigService.java   ✏️ 修改
│           └── impl/
│               └── LowPageConfigServiceImpl.java  ✏️ 修改
│
├── frontend/                          # 前端代码
│   └── src/
│       ├── api/
│       │   ├── page.ts                ✏️ 修改
│       │   ├── form.ts                ✏️ 修改
│       │   ├── table.ts               ✏️ 修改
│       │   └── button.ts              (已存在)
│       │
│       ├── components/
│       │   ├── render/                🆕 新建目录
│       │   │   ├── SearchFieldRender.vue       🆕
│       │   │   ├── FormFieldRender.vue         🆕
│       │   │   ├── TableColumnRender.vue       🆕
│       │   │   ├── SearchAreaRender.vue        🆕
│       │   │   ├── ButtonRenderer.vue          🆕
│       │   │   ├── FormRender.vue              🆕
│       │   │   └── TableRender.vue             🆕
│       │   │
│       │   └── layout/                🆕 新建目录
│       │       ├── TopBottomLayout.vue         🆕
│       │       ├── TreeTableLayout.vue         🆕
│       │       ├── TabsLayout.vue              🆕
│       │       ├── DashboardLayout.vue         🆕
│       │       └── CustomLayout.vue            🆕
│       │
│       ├── views/
│       │   ├── lowcode/
│       │   │   ├── PageRender.vue              🆕
│       │   │   ├── PageManage.vue              ✏️ 修改
│       │   │   ├── FormDesigner.vue            (已存在)
│       │   │   └── TableDesigner.vue           (已存在)
│       │   │
│       │   └── system/
│       │       └── MenuManage.vue              ✏️ 修改
│       │
│       ├── utils/
│       │   ├── dialogManager.ts                🆕
│       │   ├── drawerManager.ts                🆕
│       │   ├── configCache.ts                  🆕
│       │   ├── permissionChecker.ts            🆕
│       │   ├── dataLinkage.ts                  🆕
│       │   ├── buttonActionHandler.ts          ✏️ 修改
│       │   └── request.ts                      (已存在)
│       │
│       ├── stores/
│       │   ├── menu.ts                         (已存在)
│       │   └── user.ts                         🆕
│       │
│       ├── router/
│       │   └── index.ts                        ✏️ 修改
│       │
│       └── App.vue                             ✏️ 修改
│
└── docs/                              # 文档
    ├── migration/
    │   └── 002_add_page_publish.sql    🆕
    │
    ├── 页面配置渲染功能测试指南.md      🆕
    ├── 页面配置渲染功能实施总结.md      🆕
    ├── 页面配置渲染功能快速入门.md      🆕
    └── 页面配置渲染功能文件清单.md      🆕 (本文件)
```

## 📊 统计信息

| 类型 | 数量 |
|------|------|
| 新建文件 | 32 |
| 修改文件 | 10 |
| 删除文件 | 0 |
| **总计** | **42** |

## 📝 详细清单

### 1. 后端文件 (4个修改)

#### ✏️ LowPageConfig.java
**路径**: `backend/src/main/java/com/lowcode/entity/LowPageConfig.java`

**修改内容**:
- 添加 `routePath` 字段（路由路径）
- 添加 `published` 字段（发布状态）
- 添加 `publishTime` 字段（发布时间）

#### ✏️ PageConfigController.java
**路径**: `backend/src/main/java/com/lowcode/controller/PageConfigController.java`

**修改内容**:
- 新增 `POST /api/page/{id}/publish` 接口
- 新增 `POST /api/page/{id}/unpublish` 接口
- 新增 `GET /api/page/published` 接口

#### ✏️ ILowPageConfigService.java
**路径**: `backend/src/main/java/com/lowcode/service/ILowPageConfigService.java`

**修改内容**:
- 新增 `publishPage()` 方法定义
- 新增 `unpublishPage()` 方法定义
- 新增 `getPublishedPages()` 方法定义

#### ✏️ LowPageConfigServiceImpl.java
**路径**: `backend/src/main/java/com/lowcode/service/impl/LowPageConfigServiceImpl.java`

**修改内容**:
- 实现 `publishPage()` 方法
- 实现 `unpublishPage()` 方法
- 实现 `getPublishedPages()` 方法
- 添加路由重复检查逻辑

---

### 2. 前端API文件 (3个修改)

#### ✏️ page.ts
**路径**: `frontend/src/api/page.ts`

**修改内容**:
- PageConfig 接口新增字段：routePath, published, publishTime
- 新增 `publishPage()` 方法
- 新增 `unpublishPage()` 方法
- 新增 `getPublishedPages()` 方法

#### ✏️ form.ts
**路径**: `frontend/src/api/form.ts`

**修改内容**:
- 新增 `getButtonsByFormId()` 方法

#### ✏️ table.ts
**路径**: `frontend/src/api/table.ts`

**修改内容**:
- 新增 `getButtonsByTableId()` 方法

---

### 3. 渲染器组件 (7个新建)

#### 🆕 SearchFieldRender.vue
**路径**: `frontend/src/components/render/SearchFieldRender.vue`

**功能**: 查询字段渲染器，支持5种字段类型
- 输入框 (input)
- 下拉框 (select)
- 日期选择 (date)
- 日期范围 (dateRange)
- 数字输入 (number)

#### 🆕 FormFieldRender.vue
**路径**: `frontend/src/components/render/FormFieldRender.vue`

**功能**: 表单字段渲染器，支持18种字段类型
- 基础类型：input, textarea, select
- 日期类型：date, datetime, time, dateRange
- 数字类型：number, switch, slider
- 选择类型：radio, checkbox, cascader
- 上传类型：imageUpload, fileUpload
- 富文本：richtext

#### 🆕 TableColumnRender.vue
**路径**: `frontend/src/components/render/TableColumnRender.vue`

**功能**: 表格列渲染器，支持12种格式化类型
- 基础：普通文本
- 字典翻译：dict
- 日期时间：date, datetime
- 数字格式：number, currency, percent
- 展示组件：tag, switch, progress
- 媒体类型：image, link
- 自定义：custom, action

#### 🆕 SearchAreaRender.vue
**路径**: `frontend/src/components/render/SearchAreaRender.vue`

**功能**: 查询区域渲染器
- 支持 inline 模式
- 支持自定义列数
- 提供查询和重置按钮

#### 🆕 ButtonRenderer.vue
**路径**: `frontend/src/components/render/ButtonRenderer.vue`

**功能**: 按钮渲染器
- 支持所有 Element Plus 按钮类型
- 支持显示条件计算
- 支持 link 模式

#### 🆕 FormRender.vue
**路径**: `frontend/src/components/render/FormRender.vue`

**功能**: 表单渲染器
- 加载表单配置
- 初始化表单数据和验证规则
- 处理字段变化和联动
- 处理表单提交
- 支持多种模式（add, edit, view）

#### 🆕 TableRender.vue
**路径**: `frontend/src/components/render/TableRender.vue`

**功能**: 表格渲染器
- 加载表格配置
- 加载表格数据（支持分页）
- 处理选择变化
- 处理按钮动作
- 支持工具栏和行按钮

---

### 4. 页面渲染器 (1个新建)

#### 🆕 PageRender.vue
**路径**: `frontend/src/views/lowcode/PageRender.vue`

**功能**: 页面渲染器，支持多布局渲染
- 从路由获取页面ID
- 加载页面配置
- 根据布局类型渲染不同布局
- 提供全局上下文（provide）
- 处理查询、刷新等操作

---

### 5. 布局组件 (5个新建)

#### 🆕 TopBottomLayout.vue
**路径**: `frontend/src/components/layout/TopBottomLayout.vue`

**功能**: 上下布局（标准列表页）
- 查询区插槽
- 工具栏插槽
- 内容区插槽

#### 🆕 TreeTableLayout.vue
**路径**: `frontend/src/components/layout/TreeTableLayout.vue`

**功能**: 左树右表布局
- 左侧树形结构插槽
- 右侧内容区插槽（查询+工具栏+内容）

#### 🆕 TabsLayout.vue
**路径**: `frontend/src/components/layout/TabsLayout.vue`

**功能**: 多标签页布局
- 支持动态标签页
- 支持懒加载

#### 🆕 DashboardLayout.vue
**路径**: `frontend/src/components/layout/DashboardLayout.vue`

**功能**: 仪表盘布局
- 支持数据卡片展示
- 支持自定义网格布局

#### 🆕 CustomLayout.vue
**路径**: `frontend/src/components/layout/CustomLayout.vue`

**功能**: 自定义布局
- 支持任意组件组合
- 完全可定制

---

### 6. 工具函数 (5个新建 + 1个修改)

#### 🆕 dialogManager.ts
**路径**: `frontend/src/utils/dialogManager.ts`

**功能**: 全局弹窗管理器
- 弹窗状态管理
- 表单数据加载
- 弹窗提交处理
- DialogContainer 组件

#### 🆕 drawerManager.ts
**路径**: `frontend/src/utils/drawerManager.ts`

**功能**: 全局抽屉管理器
- 抽屉状态管理
- 表单数据加载
- 抽屉提交处理
- DrawerContainer 组件

#### 🆕 configCache.ts
**路径**: `frontend/src/utils/configCache.ts`

**功能**: 配置缓存管理器
- 本地缓存配置数据
- TTL过期机制（5分钟）
- 缓存统计和清理
- loadConfigWithCache 辅助函数

#### 🆕 permissionChecker.ts
**路径**: `frontend/src/utils/permissionChecker.ts`

**功能**: 权限检查器
- hasPermission - 检查权限
- hasRole - 检查角色
- evaluateShowCondition - 评估显示条件
- permissionDirective - 权限指令

#### 🆕 dataLinkage.ts
**路径**: `frontend/src/utils/dataLinkage.ts`

**功能**: 数据联动工具
- useDebounce - 防抖函数
- useThrottle - 节流函数
- FieldLinkageHandler - 字段联动处理器
- TreeClickHandler - 树节点点击处理器
- SearchFormHandler - 查询表单处理器
- TableActionHandler - 表格操作处理器

#### ✏️ buttonActionHandler.ts
**路径**: `frontend/src/utils/buttonActionHandler.ts`

**修改内容**:
- 集成 dialogManager
- 集成 drawerManager
- 实现 openDialog() 方法
- 实现 openDrawer() 方法
- 支持模板字符串求值

---

### 7. 状态管理 (1个新建)

#### 🆕 user.ts
**路径**: `frontend/src/stores/user.ts`

**功能**: 用户状态管理
- 用户信息
- 权限列表
- 角色列表
- Token管理

---

### 8. 视图组件 (2个修改)

#### ✏️ PageManage.vue
**路径**: `frontend/src/views/lowcode/PageManage.vue`

**修改内容**:
- 新增发布状态列
- 新增路由路径列
- 新增发布/取消发布按钮
- 新增预览按钮
- 新增 handlePublish() 方法
- 新增 handleUnpublish() 方法
- 新增 handlePreview() 方法

#### ✏️ MenuManage.vue
**路径**: `frontend/src/views/system/MenuManage.vue`

**修改内容**:
- 新增组件来源选择（静态/动态）
- 新增低代码页面选择
- 新增自动填充路由按钮
- 新增 loadPublishedPages() 方法
- 新增 autoFillRoutePath() 方法

---

### 9. 路由配置 (1个修改)

#### ✏️ index.ts
**路径**: `frontend/src/router/index.ts`

**修改内容**:
- 导入 getPublishedPages API
- 在 addDynamicRoutes() 中加载已发布页面
- 动态注册低代码页面路由

---

### 10. 应用入口 (1个修改)

#### ✏️ App.vue
**路径**: `frontend/src/App.vue`

**修改内容**:
- 导入 DialogContainer
- 导入 DrawerContainer
- 挂载全局弹窗容器
- 挂载全局抽屉容器

---

### 11. 数据库迁移 (1个新建)

#### 🆕 002_add_page_publish.sql
**路径**: `docs/migration/002_add_page_publish.sql`

**内容**: 页面发布功能数据库迁移
- 添加 route_path 字段
- 添加 published 字段
- 添加 publish_time 字段
- 创建索引

---

### 12. 文档 (4个新建)

#### 🆕 页面配置渲染功能测试指南.md
**路径**: `docs/页面配置渲染功能测试指南.md`

**内容**: 完整的功能测试指南
- 测试前准备
- 功能测试清单（8个测试场景）
- 问题排查
- 调试技巧
- 测试报告模板

#### 🆕 页面配置渲染功能实施总结.md
**路径**: `docs/页面配置渲染功能实施总结.md`

**内容**: 功能实施总结
- 项目概述
- 实施成果
- 技术实现要点
- 文件清单
- 使用指南
- 性能指标
- 已知限制

#### 🆕 页面配置渲染功能快速入门.md
**路径**: `docs/页面配置渲染功能快速入门.md`

**内容**: 快速入门指南
- 核心概念
- 快速开始（7个步骤）
- 常见配置示例
- 调试技巧
- 常见问题
- 最佳实践

#### 🆕 页面配置渲染功能文件清单.md
**路径**: `docs/页面配置渲染功能文件清单.md`

**内容**: 本文件，详细的文件清单

---

## 🎯 核心文件说明

### 必须创建的文件

要使页面渲染功能正常工作，以下文件是必须的：

**最小集合**（11个核心文件）：
1. `PageRender.vue` - 页面渲染器
2. `FormRender.vue` - 表单渲染器
3. `TableRender.vue` - 表格渲染器
4. `FormFieldRender.vue` - 表单字段渲染器
5. `SearchAreaRender.vue` - 查询区渲染器
6. `TopBottomLayout.vue` - 上下布局
7. `dialogManager.ts` - 弹窗管理器
8. `drawerManager.ts` - 抽屉管理器
9. `buttonActionHandler.ts` - 按钮动作处理器
10. `user.ts` - 用户状态管理
11. `002_add_page_publish.sql` - 数据库迁移

### 可选扩展文件

以下文件提供额外功能：

**增强功能**（10个扩展文件）：
1. `SearchFieldRender.vue` - 查询字段渲染（可合并到FormFieldRender）
2. `TableColumnRender.vue` - 列格式化（可简化）
3. `ButtonRenderer.vue` - 按钮渲染（可合并）
4. `TreeTableLayout.vue` - 左树右表布局
5. `TabsLayout.vue` - 标签页布局
6. `DashboardLayout.vue` - 仪表盘布局
7. `CustomLayout.vue` - 自定义布局
8. `configCache.ts` - 配置缓存
9. `permissionChecker.ts` - 权限检查
10. `dataLinkage.ts` - 数据联动

## 📦 依赖说明

### 已有依赖（无需安装）

项目中已经包含了所有必需的依赖：
- Vue 3.4+
- Element Plus 2.5+
- Vue Router 4.2+
- Pinia 2.1+
- Axios 1.6+
- lodash-es 4.17+

### 无需新增依赖

本实施过程中**不需要**安装任何新的npm包。

## 🚀 部署检查清单

部署前请确认：

### 后端
- [ ] 执行数据库迁移脚本
- [ ] 确认 LowPageConfig 实体包含新字段
- [ ] 确认发布接口正常工作
- [ ] 确认跨域配置正确

### 前端
- [ ] 所有新建的组件文件存在
- [ ] API接口方法已添加
- [ ] 路由配置已更新
- [ ] App.vue已挂载全局容器
- [ ] 无编译错误和警告

### 功能测试
- [ ] 可以创建页面配置
- [ ] 可以发布页面
- [ ] 可以关联菜单
- [ ] 可以通过菜单访问页面
- [ ] 页面正确渲染
- [ ] 按钮动作正常执行

## 📞 支持

如有问题，请参考：
1. [快速入门指南](./页面配置渲染功能快速入门.md)
2. [测试指南](./页面配置渲染功能测试指南.md)
3. [实施总结](./页面配置渲染功能实施总结.md)
